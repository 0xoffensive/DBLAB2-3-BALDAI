@model IEnumerable<Org.Ktu.Isk.P175B602.Autonuoma.Models.MokejimasF2.MokejimasL>

@* this will also be used as page title in main layout *@
@{ ViewData["title"] = "Mokėjimas"; }

@* content header *@
<div class="main-content-header">
	<span class="title">@ViewData["title"]</span>
	<span class="actions">
		<a href="@Url.Action("Create")" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Sukurti naują</a>
	</span>
</div>

@* content itself *@
<div class="main-content-lst">
	<table class="entity-list">
		<thead>
			<tr>
				<th>
					@Html.DisplayNameFor(m => m.id_Mokejimas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.data)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.Klientas)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.suma)
				</th>
				<th>
					@Html.DisplayNameFor(m => m.busena)
				</th>
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model) {
			<tr>
				<td>
					@Html.DisplayFor(m => item.id_Mokejimas)
				</td>
				<td>
					@Html.DisplayFor(m => item.data)
				</td>
				<td>
					@Html.DisplayFor(m => item.Klientas)
				</td>
				<td>
					@Html.DisplayFor(m => item.suma)
				</td>
				<td>
					@Html.DisplayFor(m => item.busena)
				</td>
				<td>
					<a 
						href="@Url.Action("Edit", new { id = item.id_Mokejimas })" 
						class="btn btn-primary"
						><i class="fa-solid fa-pen-to-square"></i> Redaguoti</a>
						
					<a 
						href="@Url.Action("Delete", new { id = item.id_Mokejimas })" 
						class="btn btn-danger"
						><i class="fa-solid fa-trash"></i> Šalinti</a>					
				</td>
			</tr>
			}
		</tbody>
	</table>
</div>